package com.example.domain.usecases

import com.example.domain.repository.AttendanceRepository
import javax.inject.Inject

class GetLiveAttendanceByTime @Inject constructor(private val attendanceRepository: AttendanceRepository){
//    suspend fun invoke(dto:)
}